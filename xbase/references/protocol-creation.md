# 创建协议

> 输入：探测阶段的四态分类结果 + 候选路径。输出：核心文件（创建/更新/迁移）。副作用：文件写入 + SKILL-STATE.md 状态更新。

根据探测阶段判定的四态，对每个核心文件执行对应处理：

| 状态 | 处理 |
|------|------|
| 已就绪 | 跳过，直接写入状态 |
| 需更新 | 按 skill 声明的更新范围增量刷新 |
| 迁移候选 | 保留原始内容，套用新格式 |
| 需创建 | 用 `artifact-create.py` 创建骨架，再填充 |

以下为各核心文件的创建声明：
